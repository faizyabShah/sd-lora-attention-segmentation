# Historical experiment entry points

These are the original standalone SD 1.5 and SDXL training, generation, and evaluation scripts, preserved without changing their experiment logic. Run them from the repository root if reproducing their historical outputs, because their paths are relative to that directory. The active five-seed SD 1.5 workflow uses `../cityscapes_workflow.py` and the Slurm jobs in the repository root.
